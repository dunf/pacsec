# Pacsec - Arch Linux Package Security
Pacsec is a tool that uses the [Arch Security Tracker](https://security.archlinux.org/) to audit installed packages. 
## Dependencies:
python >= 3.5   
python-requests
